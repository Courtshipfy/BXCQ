# 项目全局规范

## UI 必须所见即所得

- `.tscn` 场景和 Theme 资源是 UI 几何布局的唯一真相。`Control` 的位置、尺寸、锚点、偏移、最小尺寸、缩放和字号应在编辑器中完成并可直接预览。
- 运行时脚本不得在 `_Ready()`、`_Process()` 或普通状态切换中随意重写场景已保存的 UI 位置和尺寸。需要调整外观时，优先修改场景、容器、锚点或 Theme，不得用硬编码坐标模拟编辑器布局。
- 文字、图片内容、显隐、禁用、选中等状态可以由逻辑控制，但不得附带修改无关节点的几何布局。
- 只有明确属于功能需求的自适应或动画节点可以由脚本改变几何属性。例外必须同时满足：节点范围明确、代码中有说明、基于场景保存值或显式导出参数计算、不会覆盖其他 UI 节点，并有回归测试。
- 禁止用原始美术画布尺寸、隐藏比例或未经说明的常量，在运行时重新计算整套 UI 的位置与大小。
- 修改 UI 脚本时必须检查：编辑器中的布局值在实例化后是否保持不变。任何非自适应节点都应满足实例化前后位置和尺寸一致。
